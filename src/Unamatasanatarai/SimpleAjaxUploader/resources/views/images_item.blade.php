<div class="sau-item" data-name="{{ $name }}">
    <span class="listendelete">{{ $removeText }}</span>
    <input type="hidden" class="sau-filepath" name="{{ $name }}[]" value="{{ $src }}">
    <img src="{{ $url }}" class="hide">
    <div class="loading"></div>
</div>
